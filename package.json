{
  "name": "mcp-server-generator",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test:unit && npm run test:smoke && npm run build",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "dev": "tsx src/index.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "start": "node dist/index.js",
    "test": "tsx --test \"src/tests/**/*.test.ts\"",
    "test:integration": "tsx --test \"src/tests/**/*.integration.test.ts\"",
    "test:unit": "tsx --test \"src/tests/**/*.unit.test.ts\"",
    "test:smoke": "tsx --test \"src/tests/**/*.smoke.test.ts\"",
    "test:dsl": "tsx --test \"src/tests/dsl/*.unit.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "mcp",
    "rocketchat",
    "openapi"
  ],
  "author": "sezallagwal",
  "description": "Generates minimal MCP servers for Rocket.Chat APIs",
  "dependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "openapi-types": "^12.1.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/json-schema": "^7.0.15",
    "@types/node": "^22.15.30",
    "eslint": "^10.4.0",
    "prettier": "^3.8.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.60.0"
  }
}
