name: CI

on:
  push:
    branches: [main]
  # Run on every PR, including stacked PRs that target feature branches (not
  # only PRs into main), so format/lint/typecheck/test/build issues are caught
  # early while the repo grows.
  pull_request:

permissions:
  contents: read

jobs:
  check:
    # Windows is in the matrix because these checks are line-ending sensitive:
    # Prettier expects LF, and generated output embeds whatever the host's
    # checkout produced. A Linux-only matrix could not observe either problem.
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: [22]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }} on ${{ matrix.os }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm run test

      - name: Build
        run: npm run build
